# Emulator Bypass Patch Database
Digunakan oleh script bypass otomatis.  
Jangan hapus file `version.json` dan folder `patches/`.

## Cara update patch versi baru:
1. Buat file baru `patches_OB54.json`
2. Update `version.json` dengan hash baru
3. Commit ke branch `main`
